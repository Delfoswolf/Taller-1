var temperaturaCelsius = 5; 

if (temperaturaCelsius < 0) {
  alert("La temperatura esta por debajo de cero grados Celsius.");
} else if (temperaturaCelsius > 0) {
  alert("La temperatura esta por encima de cero grados Celsius.");
} else {
  alert("La temperatura es igual a cero grados Celsius.");
}
